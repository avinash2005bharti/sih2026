/** Text-LLM quality tier for translation: `plus` (highest quality) or `lite` (faster). Defaults to the service-configured tier. */
export declare const DubbingModelTier: {
    readonly Plus: "plus";
    readonly Lite: "lite";
};
export type DubbingModelTier = (typeof DubbingModelTier)[keyof typeof DubbingModelTier];
