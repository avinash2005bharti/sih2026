export interface DocAiUsage {
    /** Total pages in the job. */
    pages_total?: number | undefined;
    /** Pages processed so far. */
    pages_processed?: number | undefined;
    /** Pages processed successfully. */
    pages_succeeded?: number | undefined;
    /** Pages that failed processing. */
    pages_failed?: number | undefined;
}
