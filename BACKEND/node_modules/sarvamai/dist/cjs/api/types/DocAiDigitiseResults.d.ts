import type * as SarvamAI from "../index.js";
/**
 * Job results for the `digitise` pipeline.
 */
export interface DocAiDigitiseResults {
    /** The unique identifier of the job. */
    job_id: string;
    /** Terminal status of the job: `completed`, `partially_completed`, `failed`, or `rejected`. */
    status: string;
    usage: SarvamAI.DocAiUsage;
    /** One entry per input document, each holding per-page content. */
    documents: DocAiDigitiseResults.Documents.Item[];
}
export declare namespace DocAiDigitiseResults {
    type Documents = Documents.Item[];
    namespace Documents {
        interface Item {
            file_name?: string | undefined;
            pages?: Item.Pages.Item[] | undefined;
        }
        namespace Item {
            type Pages = Pages.Item[];
            namespace Pages {
                interface Item {
                    page_number?: number | undefined;
                    /** Page content in the requested output_format (HTML or Markdown). */
                    content?: string | undefined;
                }
            }
        }
    }
}
