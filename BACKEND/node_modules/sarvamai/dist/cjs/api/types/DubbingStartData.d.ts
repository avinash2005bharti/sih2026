export interface DubbingStartData {
    job_id: string;
    /** Job status after starting, e.g. `processing`. */
    status: string;
    /** Identifier of the submitted pipeline task. */
    task_id?: string | undefined;
}
