export interface ImageUrl {
    url: string;
    detail?: ImageUrl.Detail | undefined;
}
export declare namespace ImageUrl {
    const Detail: {
        readonly Auto: "auto";
        readonly Low: "low";
        readonly High: "high";
    };
    type Detail = (typeof Detail)[keyof typeof Detail];
}
