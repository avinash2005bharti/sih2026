/**
 * Model IDs accepted by `POST /v2/chat/completions`:
 *
 * - **`sarvam-105b`** — Sarvam flagship chat model.
 * - **`glm5.2`** — open-source; 512K context, tool calling, visible reasoning.
 * - **`gemma4`** — open-source; image input, tool calling.
 * - **`deepseekv4-flash`** — open-source; 1M context, tool calling, reasoning.
 *
 * `GET /v2/models` is environment-specific and may omit IDs that completions still accept.
 */
export declare const V2ModelIds: {
    readonly Sarvam105B: "sarvam-105b";
    readonly Glm52: "glm5.2";
    readonly Gemma4: "gemma4";
    readonly Deepseekv4Flash: "deepseekv4-flash";
};
export type V2ModelIds = (typeof V2ModelIds)[keyof typeof V2ModelIds];
