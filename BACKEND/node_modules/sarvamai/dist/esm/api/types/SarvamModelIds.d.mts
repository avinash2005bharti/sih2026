/**
 * Supported chat completion model IDs on `/v1/chat/completions`:
 *
 * - `sarvam-105b` — flagship model built by Sarvam and tuned for Indian languages; best for complex reasoning, coding, and agentic workflows.
 * - `sarvam-105b-conversations` — conversational variant post-trained for real-time dialogue and voice-agent use cases with natural, colloquial Indic responses.
 */
export declare const SarvamModelIds: {
    readonly Sarvam105B: "sarvam-105b";
    readonly Sarvam105BConversations: "sarvam-105b-conversations";
};
export type SarvamModelIds = (typeof SarvamModelIds)[keyof typeof SarvamModelIds];
